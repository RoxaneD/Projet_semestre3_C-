# test instruction lire
# résultat attendu : 
# a = (valeur rentrée par l'utilisateur) 
# b = (valeur rentrée par l'utilisateur)

procedure principale()
    lire (a, b)
finproc

# Code en c++ pour vérifier (à c/c)
#   cout << "a : " << a << endl;
#   cout << "b : " << b;