# Erreurs testées : 
#### (accumulable)
# pas de première parenthèse après le pour                      (accumulable)
# pas de premier point virgule avec une premiere affectation    (accumulable)
# pas de condition dans la parenthèse                           (accumulable)
# pas le deuxième point virgule                                 (accumulable)
# pas de dernière parenthèse après le pour                      (accumulable)
# pas d'instructions après dernière parenthèse du pour          (accumulable)

# Erreurs qu'on ne peut pas cumuler :
####
# On ne peut pas obtenir d'erreur claire s'il n'y a rien du tout après le pour
# On ne peut pas obtenir d'erreur claire s'il n'y a pas la premier point virgule sans premiere affectation

procedure principale()
    i = 16;
    j = 1;

    pour (j = 1;j<5; j = j+1)
        i=i-1;
    finpour

    pour (j = 0;j<10; j = j+1)
        i=i-1;
    finpour


finproc