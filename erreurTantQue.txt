# Erreurs testées : 
####
# pas la premiere parenthèse apres tant que (accumulable)
# rien dans la parenthèse                   (accumulable)
# pas la dernière parenthèse apres tant que (accumulable)
# pas d'instruction après le tant que       (accumulable)

# Erreurs qu'on ne peut pas cumuler :
####
# On ne peut pas obtenir d'erreur claire s'il n'y a rien du tout entre tant que et les instructions

procedure principale()

    i = 16;
    tantque (i>10)
       i = i-1;  
       k = 50-i;
       j = k+i;
    fintantque

    tantque (i>5)
       i = i-1;  
       k = 50-i;
       j = k+i;
    fintantque
finproc